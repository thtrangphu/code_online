1.Được tái sử dụng nhất là khi công việc đó lặp lại nhiều lần, mỗi hàm làm một nhiệm vụ rất rõ ràng cụ thể
2.2 loại hàm: trả về và không trả về
  +Trả về: trả về một số để thực hiện tính toán ở các bước tiếp theo hoặc là kết quả của biểu thức cần xuất ra màn hình
  +Không trả về: xuất kết quả ra màn hình
 3.return thoát hàm và trả về giá trị của hàm 
 4.tham số thực: giá trị của biến khi đang chạy
   tham số hình thức: nằm ở phần định nghĩa nhằm liệt kê các biến
 5.tham trị: tạo bản sao, khi thay đổi giá trị thì không ảnh hưởng đến giá trị ban đầu
  tham chiếu: đi đến địa chỉ của biến nên nếu thay đổi giá trị thì nó sẽ cật nhập lại
 6.khi có lời gọi hàm trong hàm main
 7.dùng make_tuple(bien1,bien2,...) hoặc theo em nghĩ cách hay dùng thì viết thành 2 hàm để trả về 2 giá trị
 
